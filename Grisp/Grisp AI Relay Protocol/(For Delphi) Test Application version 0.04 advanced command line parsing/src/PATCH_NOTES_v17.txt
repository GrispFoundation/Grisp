GARP/1.24 Delphi Client v17

Fix:
- Added Indy IdException to the GARP124.Client uses list so EIdReadTimeout is declared.
- Retains v16 behavior: ReceiveUntilPromptEvent treats the 1-second EIdReadTimeout polling interval as a normal wait and continues until the overall timeout or a matching terminal event.
