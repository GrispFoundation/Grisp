GARP/1.24 Delphi Client v0.05

Changes:
- Extended the live --conformance smoke test from one prompt to two prompts on the same session.
- Verifies the session returns to READY after the first GENERATION_COMPLETED.
- Reuses the exact same prompt text for prompt #2 so the second response text is identical to the first.
- Verifies the two prompt request IDs are distinct.
- Verifies prompt #2 reaches GENERATION_COMPLETED on the existing session.
- Verifies prompt #2 returns the same response text as prompt #1.
- Verifies the session remains READY after prompt #2 before CLOSE_SESSION.
- Bumped the Delphi test-client release to v0.05. GARP semantic/wire protocol remains 1.24 / 0x00.
