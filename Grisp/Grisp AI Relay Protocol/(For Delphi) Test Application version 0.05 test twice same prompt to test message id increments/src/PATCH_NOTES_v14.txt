GARP/1.24 Delphi Client v14

Baseline correction:
- Reconciled GARP124.Client.pas against the supplied v0.03 client behavior.
- Preserved the newer compiler compatibility fixes in the current client.

Restored/fixed from v0.03:
- HELLO payload ownership is transferred immediately after BuildMessage, before SendFrame can raise.
- HELLO_AUTH payload ownership is transferred immediately after BuildMessage, before SendFrame can raise.
- ReceiveUntilRequest returns CAPABILITIES/BROWSER_STATUS when the caller explicitly waits for request_id=''.
- CreateSession again reads the returned session state and waits for SESSION_READY for PREPARING/AUTH_REQUIRED/RATE_LIMITED states.
- FServerName is retained as a client field and populated from HELLO_CHALLENGE for proof construction.

Retained compatibility fixes:
- No Winapi.BCrypt unit dependency; BCryptGenRandom is dynamically imported from bcrypt.dll.
- No TTimeZone dependency; timestamp is generated directly from UTC SYSTEMTIME.
- No DateToISO8601 dependency.
- Negotiated-feature membership uses an explicit helper rather than the dynamic-array 'in' operator.
- Raw Windows command-line parsing remains in GARP124.Test.dpr.
