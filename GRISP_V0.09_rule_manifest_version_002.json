{
  "version": "v0.09",
  "generated_by": "Skybuck",
  "generated_at": "2026-06-09T23:33:00Z",
  "rules": [
    {
      "id": "MergeDuplicatePlannedTasks",
      "description": "Detect and merge duplicate planned tasks created by the same planning run or with identical normalized descriptions.",
      "priority": 95,
      "idempotent": true,
      "sideEffectFlags": [],
      "timeout_ms": 2000,
      "rate_limit": { "max_per_tick": 100 },
      "circuit_breaker": { "failure_threshold": 50, "cooldown_ms": 600000, "half_open_attempts": 5 },
      "depends_on": [],
      "after_commit": [],
      "reads": ["Task.lifecycle", "Task.description", "Task.created_at", "Task.tags", "created_by_plan"],
      "writes": ["Task.tags", "Task.cost", "depends_on_edges", "belongs_to_goal_edges"],
      "notes": "Engine may require explicit writes when inference is ambiguous."
    },
    {
      "id": "DetectPlannedCycle",
      "description": "Detect cycles among planned tasks and mark them for human review or break cycles according to policy.",
      "priority": 94,
      "idempotent": true,
      "sideEffectFlags": [],
      "timeout_ms": 1000,
      "rate_limit": { "max_per_tick": 100 },
      "circuit_breaker": { "failure_threshold": 50, "cooldown_ms": 600000, "half_open_attempts": 5 },
      "depends_on": [],
      "after_commit": [],
      "reads": ["Task.lifecycle", "depends_on"],
      "writes": ["Task.tags"],
      "notes": "Marks tasks with tag 'cycle_detected' or rewires dependencies per policy."
    },
    {
      "id": "ActivatePlannedTask",
      "description": "Flip a planned task to pending when all blocking dependencies are not in-progress/pending/assigned/waiting_tool.",
      "priority": 90,
      "idempotent": true,
      "sideEffectFlags": [],
      "timeout_ms": 500,
      "rate_limit": { "max_per_tick": 500 },
      "circuit_breaker": { "failure_threshold": 100, "cooldown_ms": 60000, "half_open_attempts": 3 },
      "depends_on": ["MergeDuplicatePlannedTasks", "DetectPlannedCycle"],
      "after_commit": [],
      "reads": ["Task.lifecycle", "depends_on"],
      "writes": ["Task.lifecycle", "Task.updated_at"],
      "notes": "Pure graph rewrite; no external calls."
    },
    {
      "id": "AssignPendingTask",
      "description": "Assign a pending task to a capable specialist if capacity allows; update capacity_used and create assigns edge.",
      "priority": 80,
      "idempotent": false,
      "sideEffectFlags": [],
      "timeout_ms": 1000,
      "rate_limit": { "max_per_tick": 200, "max_per_second": 1000 },
      "circuit_breaker": { "failure_threshold": 20, "cooldown_ms": 60000, "half_open_attempts": 3 },
      "depends_on": ["ActivatePlannedTask"],
      "after_commit": [],
      "reads": ["Task.lifecycle", "Task.cost", "Agent.capacity_total", "Agent.capacity_used", "Agent.capabilities"],
      "writes": ["Task.lifecycle", "Agent.capacity_used", "assigns_edge"],
      "notes": "Engine must apply this atomically to avoid double-assignment."
    },
    {
      "id": "StartAssignedTask",
      "description": "Transition an assigned task to in_progress when the assignee is ready to start.",
      "priority": 70,
      "idempotent": true,
      "sideEffectFlags": [],
      "timeout_ms": 500,
      "rate_limit": { "max_per_tick": 500 },
      "circuit_breaker": { "failure_threshold": 50, "cooldown_ms": 60000, "half_open_attempts": 3 },
      "depends_on": ["AssignPendingTask"],
      "after_commit": [],
      "reads": ["Task.lifecycle", "assigns"],
      "writes": ["Task.lifecycle", "Task.updated_at"],
      "notes": "Lightweight transition; used to trigger CreateToolRequest rules."
    },
    {
      "id": "CreateToolRequest",
      "description": "Create a ToolRequest for an in_progress task when required; set request_key for idempotency.",
      "priority": 60,
      "idempotent": false,
      "sideEffectFlags": ["creates_request", "external_call"],
      "timeout_ms": 5000,
      "rate_limit": { "max_per_tick": 200, "max_per_second": 1000 },
      "circuit_breaker": { "failure_threshold": 10, "cooldown_ms": 120000, "half_open_attempts": 2 },
      "depends_on": ["StartAssignedTask"],
      "after_commit": ["schedule_timeout_for_tool_request"],
      "reads": ["Task.lifecycle", "Task.description", "assigns"],
      "writes": ["ToolRequest.node", "Task.lifecycle", "tool_for_task_edge"],
      "notes": "Must provide request_key; engine deduplicates by request_key."
    },
    {
      "id": "TimeoutStaleRequests",
      "description": "Engine-enforced rule: mark pending ToolRequest/PlanningRequest as failed after timeout_ms and emit events.",
      "priority": 100,
      "idempotent": true,
      "sideEffectFlags": [],
      "timeout_ms": 0,
      "rate_limit": { "max_per_tick": 1000 },
      "circuit_breaker": { "failure_threshold": 1000, "cooldown_ms": 60000, "half_open_attempts": 1 },
      "depends_on": [],
      "after_commit": [],
      "reads": ["ToolRequest.status", "ToolRequest.created_at", "PlanningRequest.status", "PlanningRequest.created_at"],
      "writes": ["ToolRequest.status", "PlanningRequest.status"],
      "notes": "Engine should implement this as built-in behavior using declared timeout_ms on requests."
    },
    {
      "id": "IntegrateToolResultSuccess",
      "description": "Integrate a successful ToolResult into its task: mark task completed, attach result, release capacity.",
      "priority": 95,
      "idempotent": true,
      "sideEffectFlags": [],
      "timeout_ms": 2000,
      "rate_limit": { "max_per_tick": 500 },
      "circuit_breaker": { "failure_threshold": 50, "cooldown_ms": 60000, "half_open_attempts": 3 },
      "depends_on": ["CreateToolRequest"],
      "after_commit": [],
      "reads": ["ToolResult.success", "ToolResult.request", "ToolRequest.status", "Task.lifecycle", "assigns"],
      "writes": ["Task.lifecycle", "Task.result", "Agent.capacity_used", "assigns_edge"],
      "notes": "Must atomically update capacity_used and delete assigns edge when releasing."
    },
    {
      "id": "IntegrateToolResultFailure",
      "description": "Handle failed ToolResult: increment retries, replan or mark task failed based on max_retries.",
      "priority": 90,
      "idempotent": true,
      "sideEffectFlags": [],
      "timeout_ms": 2000,
      "rate_limit": { "max_per_tick": 500 },
      "circuit_breaker": { "failure_threshold": 50, "cooldown_ms": 60000, "half_open_attempts": 3 },
      "depends_on": ["CreateToolRequest"],
      "after_commit": [],
      "reads": ["ToolResult.success", "ToolResult.request", "ToolRequest.status", "Task.lifecycle", "Task.retries", "Task.max_retries"],
      "writes": ["Task.lifecycle", "Task.retries", "Task.error", "Agent.capacity_used", "assigns_edge"],
      "notes": "If retries remain, task returns to planned; otherwise mark failed and release capacity."
    },
    {
      "id": "ReleaseCapacityOnCompletion",
      "description": "Ensure agent capacity is released when tasks complete or fail and assigns edge is removed.",
      "priority": 85,
      "idempotent": true,
      "sideEffectFlags": [],
      "timeout_ms": 500,
      "rate_limit": { "max_per_tick": 1000 },
      "circuit_breaker": { "failure_threshold": 100, "cooldown_ms": 60000, "half_open_attempts": 3 },
      "depends_on": ["IntegrateToolResultSuccess", "IntegrateToolResultFailure"],
      "after_commit": [],
      "reads": ["Task.lifecycle", "Task.cost", "assigns"],
      "writes": ["Agent.capacity_used", "assigns_edge"],
      "notes": "Idempotent: safe to reapply if capacity accounting is guarded."
    },
    {
      "id": "SynthesizeLeafGoalResults",
      "description": "When a leaf goal's tasks are all completed, synthesize a FinalAnswer and mark the goal completed.",
      "priority": 50,
      "idempotent": true,
      "sideEffectFlags": ["external_call"],
      "timeout_ms": 5000,
      "rate_limit": { "max_per_tick": 50 },
      "circuit_breaker": { "failure_threshold": 10, "cooldown_ms": 60000, "half_open_attempts": 2 },
      "depends_on": ["IntegrateToolResultSuccess", "ReleaseCapacityOnCompletion"],
      "after_commit": [],
      "reads": ["Goal.lifecycle", "belongs_to_goal", "Task.lifecycle"],
      "writes": ["Goal.lifecycle", "FinalAnswer.node"],
      "notes": "MergeResults may be an effectful function (LLM); declare @effect and idempotent=true where possible."
    },
    {
      "id": "RebalancePlannedTasks",
      "description": "Optional optimizer: rebalance planned tasks across agents by adjusting tags/costs or suggesting reassignments.",
      "priority": 30,
      "idempotent": true,
      "sideEffectFlags": [],
      "timeout_ms": 2000,
      "rate_limit": { "max_per_tick": 20 },
      "circuit_breaker": { "failure_threshold": 20, "cooldown_ms": 120000, "half_open_attempts": 2 },
      "depends_on": ["MergeDuplicatePlannedTasks"],
      "after_commit": [],
      "reads": ["Task.lifecycle", "Agent.capacity_total", "Agent.capacity_used", "Task.tags"],
      "writes": ["Task.tags", "Task.priority"],
      "notes": "Low-priority optimization rule; safe to run periodically."
    }
  ]
}
